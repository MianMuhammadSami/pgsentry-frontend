export const metadata = {
  title: 'Log In',
  description: 'Log in to pgSentry to access your PostgreSQL monitoring dashboard.',
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
