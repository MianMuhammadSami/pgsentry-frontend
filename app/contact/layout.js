export const metadata = {
  title: 'Contact',
  description: 'Get in touch with the pgSentry team. Questions about PostgreSQL monitoring, custom setup, or support.',
};

export default function ContactLayout({ children }) {
  return children;
}
